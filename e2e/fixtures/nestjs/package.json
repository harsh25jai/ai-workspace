{
  "name": "fixture-nestjs-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "tsc -b",
    "test": "jest"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
